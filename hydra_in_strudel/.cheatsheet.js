osc(size[1-100(small)], speed[.1-1(fast)], color_offset[.5-5(colorful)]) // Color

// Strudel
H("") //Strudel pattern in Hydra.
all(x => x) //Strudel visuals in Hydra. Needs `src(s0)` for Hydra code and `$:` for sound to be registered.
